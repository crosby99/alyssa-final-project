{% extends '_layouts/base.njk' %}

{% block page_title %} Queen {% endblock %}

{% block page_description %} Information on the band "Queen."{% endblock %}

{% block content %}

<div class="container">
  <div class="row">
    <div class="col">
          <h1> Queen </h1>
          <ul>
     {% for members in queen.members %}
  <h3 class="members-name"><a href="{{ members.url }}">{{ members.name }}</a></h3>
  <p class="members-instruments">{{ members.instruments }}</p>
  <hr>
{% endfor %}
  </ul>
    </div>
    <div>
  </div>
{% endblock %}
